\section{Методы}

\subsection{Генерация и визуализация выпуклого множества}

Генерируются 20 точек с координатами из диапазона $[-5,5]$. Функция
\texttt{ConvexHull} возвращает индексы вершин оболочки в порядке
обхода. Внутренние точки обозначены синим прозрачным цветом, а вершины
оболочки~--- красным. Сама оболочка закрашена полупрозрачным
зеленым цветом.

\inputmintedbox{python}{code/lab02-1.py}
\newpage
\textbf{Вопрос для проверки.}
\textit{Является ли полученный многоугольник выпуклым множеством согласно
определению? Ответ обоснуйте.}

\textbf{Ответ.}
Да, полученный многоугольник является выпуклым множеством. Он был построен
как выпуклая оболочка исходного набора точек с помощью функции
\texttt{ConvexHull}. По определению выпуклой оболочки это наименьшее
выпуклое множество, содержащее все исходные точки.

Следовательно, для любых двух точек $x$ и $y$, принадлежащих полученному
многоугольнику, и любого $\lambda\in[0,1]$ выполняется
\[
    \lambda x+(1-\lambda)y
    \in \operatorname{conv}(S),
\]
где $\operatorname{conv}(S)$ --- выпуклая оболочка множества исходных
точек $S$. Иными словами, отрезок, соединяющий любые две точки
многоугольника, целиком лежит внутри него.

Таким образом, полученный многоугольник удовлетворяет определению
выпуклого множества.

\newpage

\subsection{Проверка свойства выпуклости через пересечение}

Рассматриваются множества
\[
	A=\{(x,y):x^2+y^2\leq 3^2\},
\]
\[
	B=\{(x,y):(x-2)^2+(y-1)^2\leq 2.5^2\}.
\]
На сетке $[-5,5]\times[-5,5]$ с шагом $0.01$ для каждой точки вычисляются логические маски принадлежности $A$, $B$ и $A\cap B$. Три непересекающиеся категории отображаются разными цветами.

\inputmintedbox{python}{code/lab02-2.py}

\textbf{Вопрос для проверки.}
\textit{Является ли пересечение двух кругов выпуклым множеством? Всегда ли
пересечение выпуклых множеств выпукло? Ответьте, основываясь на полученном
графике и теоретическом свойстве.}

\textbf{Ответ.}
Да, пересечение двух кругов является выпуклым множеством. Каждый круг
выпуклый, поскольку для любых двух его точек отрезок, соединяющий эти
точки, целиком лежит внутри круга.

На графике пересечение кругов $A\cap B$ представляет собой линзовидную
область без выемок. Поэтому для любых двух точек этой области отрезок
между ними также принадлежит пересечению.

Согласно доказанной теореме~\ref{thm:intersection}, пересечение любого семейства
выпуклых множеств является выпуклым множеством.

Таким образом, пересечение двух кругов в данной задаче выпукло. Более
того, пересечение любого числа выпуклых множеств всегда выпукло. При этом
пересечение может быть пустым; пустое множество также считается выпуклым.

\newpage

\subsection{Проверка критерия выпуклости}

Функция \texttt{is\_convex} последовательно рассматривает все тройки соседних вершин, включая переход через последнюю вершину. Для каждой тройки вычисляется знак ориентированного произведения. Первый ненулевой знак сохраняется как эталонный; если встречается противоположный знак, функция немедленно возвращает \texttt{False}. В тестах использованы выпуклый многоугольник с коллинеарной точкой, невыпуклый многоугольник с выемкой и звёздный многоугольник.

\inputmintedbox{python}{code/lab02-3.py}
\begin{mintedbox}{python}
# Одна точка лежит на линии между двумя другими точками,
# но не нарушает выпуклость.
# Повторяющиеся точки не нарушают выпуклость
oriented_points_convex = np.array(
	[[0, 0], [1, 0], [1, 1], [0.5, 1], [0, 1], [0, 0]]
	)
oriented_points_non_convex = np.array(
	[[0, 0], [1, 0], [0.5, 0.5], [1, 1], [0, 1], [0, 0]]
	)
star_points = np.array(...)
print(is_convex(oriented_points_convex))  # True
print(is_convex(oriented_points_non_convex))  # False
print(is_convex(star_points))  # False
\end{mintedbox}


